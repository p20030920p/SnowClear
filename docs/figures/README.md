# Result figures — interface

Every experimental result in this repository has a figure slot reserved for it. This file is the
single index of them: the READMEs carry only the captions, so nothing renders as a broken image
and the homepage stays short. Published figures are live in both READMEs already.

## How to publish a figure

1. Drop the file into this directory using the **exact filename** in the table below.
2. Add one image line at the matching caption in `README.md` / `README_CN.md` — each reserved
   slot ends with `Slot: \`docs/figures/<name>.png\``:

```markdown
![Per-scene precision, recall and F1](docs/figures/fig1_per_scene.png)
```

The READMEs carry only the captions, so the homepage stays short and nothing renders as a broken
image before the file exists. To list the reserved slots:

```bash
grep -n 'Slot:' README.md README_CN.md
```

Conventions: PNG, ≥ 1600 px wide, no transparency (GitHub renders on both light and dark
themes), one figure per result, caption text kept in the document rather than burned into the
image.

## Rendered algorithm (not a result figure)

| File | Shows | Regenerated by |
|---|---|---|
| `algorithm1_en.png` / `.svg` | Algorithm 1 with English annotations, referenced by `README.md` | `python3 tools/gen_algorithm_fig.py` |
| `algorithm1_zh.png` / `.svg` | The same algorithm with Chinese annotations, referenced by `README_CN.md` | same |

The algorithm is a figure rather than a fenced code block on purpose: a code block is rendered
with whatever monospace font the reader's browser picks, and a CJK annotation on the same line
destroys the column grid. The SVG stores its text as paths, so it cannot reflow either. Edit the
content inside `tools/gen_algorithm_fig.py` and re-run it — do not retouch the images.

## Published result figures

Panel sequence, colours and the metric callouts are produced by
`tools/render_qualitative.py`; every number printed in a panel is recomputed from the index
files, and the tool prints the same precision / recall / F1 the pipeline reports.

| File | Shows | Notes |
|---|---|---|
| `fig0_rviz.png` | **The hero at the top of both READMEs**: a real RViz window running the shipped layout — de-snowed cloud in grey, removed points in red, all displays `Status: Ok` | `bash tools/capture_rviz_screenshot.sh`. Needs a display; RViz is alive ~35 s. The capture is the X11 client window via `xwd`, decoded by `tools/xwd_to_png.py`. Settings used for the still, all asset-only edits to a **copy** of `snowclear.rviz`: window 1800x1080, orbit distance 40, the dimmed raw overlay off (it sits on the same coordinates as the removed points and washes the red out), the de-snowed cloud in `FlatColor 190;202;214` instead of the rainbow intensity ramp, removed points at 8 px, and a static `map -> lidar` transform — without a TF tree RViz warns that the fixed frame does not exist even though it renders |
| `fig0_banner.png`, `fig0_zh_banner.png` | The same pair as a flat bird's-eye view | `--banner`; useful when the depth cue is not wanted (print, or a slide where the ROI circle matters) |
| `fig0_before.png`, `fig0_after.png` (+ `_zh`) | The pair that opens both READMEs: raw scan with the removed points in red, and the de-snowed result — each as full scene plus a 5.2 m zoom | `python3 tools/render_hero.py --pcd <frame> --detection <indices>`; the zoom window is chosen automatically where snow lies on visible structure, because a crop of the densest cluster is a red cloud with nothing underneath |
| `fig2_qualitative.png`, `fig2_qualitative_zh.png` (+ `.svg`) | Scene 35 `042126`: (a) raw scan, (b) annotated snow, (c) TP / FN / FP with per-frame P / R / F1 called out, (d) the de-snowed cloud | the reference frame; misses are a handful of clusters just inside the ROI |
| `fig10_qualitative_hard.png`, `fig10_qualitative_hard_zh.png` | Scene 16 `040036`: the same four panels | recall 44.1 % — most ground truth lies **outside** the ROI circle, which is why the misses are structural |
| `fig11_comparison.png`, `fig11_comparison_zh.png` | Scene 35 `042126`: SnowClear against SOR on the same frame, 2×2 | F1 96.31 vs 62.33; the baseline panel is dominated by false positives |

```bash
# one command each; the detection files come from
#   ros2 run snowclear_core snowclear_cli pcd_file:=<frame> gt_folder:=<gt> \
#       save_results:=true output_dir:=/tmp/det detector_type:=feature_fusion
python3 tools/render_qualitative.py --pcd <frame.pcd> --gt <gt.txt> \
    --detection /tmp/det/fusion/<stem>.txt --limits 26 --desnowed \
    --out docs/figures/fig2_qualitative.png
# --lang zh for the Chinese variant, --detection2 <baseline.txt> --label2 <name> for a comparison
```

## Slots

Still to be filled. These stay commented out in the READMEs until a file lands here.

| # | File | Shows | Referenced from | Regenerated by |
|---|---|---|---|---|
| 1 | `fig1_per_scene.png` | Per-scene precision / recall / F1 over the 19 mirrored scenes, 16-scene reported set marked | `README.md` / `README_CN.md` Results; `DATASET.md` §3 | `snowclear_runner --mode eval_folders pcd_root:=… result_root:=… csv_path:=per_scene.csv` |
| 3 | `fig3_comparison.png` | Precision / recall / F1 of SnowClear against DROR, DSOR, SOR, ROR | README Results (Table 2) | same command as #1 with `detector_type:=dror\|dsor\|sor\|ror` |
| 4 | `fig4_ablation.png` | Module-wise macro-F1 delta per switch, with the ROI + `I = 0` baseline as a reference line | README Results (Table 3) | one `--mode eval_folders` run per switch configuration in `config/snowclear_params.yaml` |
| 5 | `fig5_acceptance.png` | Acceptance region of the released decision function in the `(I/T, h_ag)` plane | README Results; `METHOD.md` §2 | analytic — [`METHOD.md`](../METHOD.md) §2 |
| 6 | `fig6_runtime.png` | Per-stage frame time, plus the before/after of the two equivalence-preserving optimisations | README Results (Table 5); `METHOD.md` §5; `ROS2.md` §5 | `--mode reproduce_full … verbose:=true` with `OMP_NUM_THREADS=2 OMP_DYNAMIC=false` |
| 7 | `fig7_cross_sensor.png` | Released absolute constants vs. the label-free self-calibrated replacements | README Results (Table 6); `METHOD.md` §6 | `enable_sensor_height_roi` / `enable_range_selfcal` / `enable_sparse_support_expand` runs |
| 8 | `fig8_gt_ceiling.png` | Recall ceiling from ground truth removed by the ROI gate, by scene | README Results; `DATASET.md` §4 | `--mode eval_folders` plus the ground-truth index files |
| 9 | `fig9_threshold_curve.png` | `T(r, I)` for several intensities, with `Tg = clamp(0.8·Q1, 2.5, 8.0)` and the `α(r)` Gamma curve | `METHOD.md` §3 | analytic — [`METHOD.md`](../METHOD.md) §3, one frame of `--dump-params` for `Q1` |

## What must not appear here

The dataset itself. Point clouds and their ground truth are not redistributable (see
[`../DATASET.md`](../DATASET.md)); figures may show rendered scans, but no figure may embed a
redistributable copy of the source data.

Numbers in the tables of `README.md` are the ones this repository can reproduce on its own
data. Baseline cells that require third-party harnesses are left as explicit `TODO` rather
than filled with values nobody here can regenerate.
